require 'spec_helper'

describe MyReferrer do
  it 'has a version number' do
    expect(MyReferrer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
